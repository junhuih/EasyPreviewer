services:
  app:
    container_name: modern-preview-only
    build:
      context: .
      dockerfile: Dockerfile
    image: modern-preview-only:local
    ports:
      - "8080:8080"
    environment:
      JAVA_OPTS: -Xms512m -Xmx1024m
    restart: unless-stopped
